package com.example.recallai.ui.patient

/**
 * Toggle lively care/schedule UI on the patient dashboard.
 * Set [USE_LIVELY_CARE_UI] to **false** and rebuild to restore the original
 * care tasks, Today, alarms, and reminders sections.
 */
object PatientCareUiLayout {
    const val USE_LIVELY_CARE_UI = true
}
