package com.example.recallai.recallassistant

/**
 * Toggle Recall screen visual style.
 * Set [USE_LIVELY_RECALL_UI] to **false** and rebuild to restore the original Recall screen.
 */
object RecallUiLayout {
    const val USE_LIVELY_RECALL_UI = true
}
