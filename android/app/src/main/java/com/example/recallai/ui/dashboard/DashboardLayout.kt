package com.example.recallai.ui.dashboard

/**
 * Toggle dashboard visual style.
 * Set [USE_MINDCARE_STYLE] to **false** and rebuild to restore the original glass/medal dashboards
 * ([PatientHomeScreenLegacy] / [CaregiverHomeScreenLegacy]).
 */
object DashboardLayout {
    const val USE_MINDCARE_STYLE = true
}
